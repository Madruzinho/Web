import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    color: '#e5bf3c'
  },
  imagem: {
    resizeMode: 'contain',
    height: 200,
  }
});