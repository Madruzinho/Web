import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    color: '#000000'
  },
  imagem: {
    resizeMode: 'contain',
    height: 200,
  }
});